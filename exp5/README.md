# Experiment 5

Files:
- `exp5.py` — Main experiment script for Experiment 5.

Description:
Experiment 5 contains a single script `exp5.py`. Inspect the script to understand its inputs and behavior. It may read external data or perform processing similar to the other experiments.

Usage:

```
pip install pandas numpy scikit-learn
python exp5.py
```

Notes:
- If `exp5.py` requires data files, ensure they are placed in the same folder or update the script's paths.