# Experiment 1

Files:
- `exp1.py` — Main experiment script for Experiment 1.
- `diabetes.csv` — Dataset used by the experiment (diabetes dataset).

Description:
This folder contains a small data science experiment that reads `diabetes.csv`, performs preprocessing, trains a model, and reports evaluation metrics.

Usage:
1. Create a Python environment and install dependencies (typical):

```
pip install pandas numpy scikit-learn
```

2. Run the experiment:

```
python exp1.py
```

Notes:
- Adjust paths in `exp1.py` if running from a different working directory.
- If the script expects additional packages, install them as needed.